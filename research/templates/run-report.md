# Run report

## Identity
- Task and run ID:
- Date / operator:
- Git commit / environment lock:
- Command:
- Configuration path and SHA-256:
- Hardware / software:
- Checkpoint origin and SHA-256:
- Pretraining / external data:

## Data and protocol
- Dataset version / authorization register entry:
- Manifest and split SHA-256:
- Independent unit / train-dev-test counts:
- Subject, session, video overlap and duplicate checks:
- Label mapping / score direction:
- Crop, resize, orientation, channel order, normalization:
- Model selection, threshold selection and calibration:
- Attack decision at threshold (including equality):
- Source-only development rule for external targets:

## Evidence
- Restricted score file location and SHA-256:
- APCER / BPCER / ACER with denominators:
- Attack types / acquisition conditions:
- Seed(s) / uncertainty / exclusions:
- Runtime and latency protocol, if relevant:
- Reproduction command / logs:

## Interpretation
- Research question answered:
- Observed result and comparison:
- Limitations / failed checks:
- Next decision:
- Reviewer and review outcome:
